'use client'
import PageLayout from "../../../public/pageLayout";

// `app/tasks/page.tsx` is the UI for the `/tasks` URL
export default function Page() {
    return (
        <PageLayout>
            <h1>Task info page</h1>
        </PageLayout>
    )
}