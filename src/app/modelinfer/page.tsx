'use client'
import PageLayout from "../../../public/pageLayout";

// `app/modelinfer/page.tsx` is the UI for the `/modelinfer` URL
export default function Page() {
    return (
        <PageLayout>
            <h1>Model infer page</h1>
        </PageLayout>
    )
}