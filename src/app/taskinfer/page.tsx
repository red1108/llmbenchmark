'use client'
import PageLayout from "../../../public/pageLayout";

// `app/taskinfer/page.tsx` is the UI for the `/taskinfer` URL
export default function Page() {
    return (
        <PageLayout>
            <h1>Task infer page</h1>
        </PageLayout>
    )
}